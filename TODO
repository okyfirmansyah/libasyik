GENERAL/Service:
-make aixlog sync asynchronous(sync all sink to a dedicated thread)

HTTP:
-delete SSL verify_none
-Support SSL client custom context(for e.g custom certificate, paths, callbacks)
-SSL detailed error response
-http test network negative case(host not found, dns not found, no connection, timeouts)
-http large request and large response
-make http full client(separate connection and request)
-set global timeout
  WEBSOCKET:
  -support binary websocket
  -ability for websocket to add custom header

SOCI:
-rapihin SOCI's CMakeList
-test postgresql

Important Modules:
-Socket TCP/UDP
-Redis/Memcache
-RabbitMQ
